Task: Ice-cream order FSM. New pure FSM src/game/mission/iceCreamMission.ts (idle->spawned->driving->active->complete, SERVE_RANGE 1.9, single-serve, 2.5s linger, patient-forever) mirroring missionManager.ts. Tests: src/game/mission/iceCreamMission.test.ts, 8 passed, module coverage ~97%. Why: second mission needs its own state so fire and ice-cream never share mutable mission state; shared busy gate comes next.
