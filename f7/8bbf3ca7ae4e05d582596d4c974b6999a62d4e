Phase 2 Tasks 1+2: Generic mission FSM core + abort semantics (FR1, FR6). Created src/game/mission/missionFsm.ts: declared-states config, guarded attempt(from,to,when) with one-transition-per-update/tap lock, tick/tap delegation (celebrating-state linger owns frames), once-per-entry onCelebrate, abort() from any state with onAbort cleanup + timer reset. Red-first: 28 contract tests (declared states/guards/delegation/lock/celebration-once/linger/abort-from-every-state/per-mission config smoke with real state sets + linger constants). Coverage missionFsm.ts: 100% stmts/branch/funcs. Abort tests were written in the same red batch as core tests, so Task 2 shares this commit. Gates: pnpm check, pnpm typecheck, CI=true pnpm test green (568 tests / 46 files).
