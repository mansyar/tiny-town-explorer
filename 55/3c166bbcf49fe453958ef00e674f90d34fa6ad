VehicleSystem landed as pure logic (18 tests, 100% statements/lines, 90% branch). The fleet registry maps each id to its registry model; abilities emit typed events (spray with seconds, jingle+cones, gulp, siren); engineRate is a clamped linear map from speed to ENGINE_IDLE_RATE..ENGINE_TOP_RATE; the spray cast starts a 1.5s burst that update() expires, interruptBurst() cancels and setActive() abandons, and requestAbility() returns no events while one is in flight.
