Task: Mount the two movers (FR1, FR2)

Changes: mountTrafficActors (trafficActors.ts) mounts PARKED_CAR_MODELS[pose.kind] through createVehicleActor at fitLength PARKED_CAR_FIT 0.55 / facingYaw FLEET_FACING_YAW, castShadow off on every mesh; trafficSystem publishes read-only TrafficPose mirrors (id, kind, live position, heading, bounceProgress) via poses(); vehicleActor parameter narrowed from VehicleMotor to the new VehiclePose (all it ever read). Files: src/game/traffic/trafficActors.ts, src/game/traffic/trafficSystem.ts, src/game/traffic/trafficSystem.test.ts, src/game/vehicle/vehicleActor.ts. Why: FR1 mounting needs poses without unsealing the motors (FR9) - the actor only ever copied a pose, so VehiclePose is its honest parameter and the mirror can never steer a mover. Car Kit faces +z (kit-mount-measurements.md), so FLEET_FACING_YAW 0 rather than MODEL_FACING_YAW pi: invisible on parked cars, glaring on movers. Measured: 2032 + 2088 = 4120 model tris. Manual steps (plan: recorded in the commit note; they run at the Phase 6 wiring pass): pnpm dev, watch two cars trundle and turn smoothly at corners.
