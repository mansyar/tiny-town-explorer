/**
 * Application entry point.
 *
 * Phase 1 boots an empty town ground as a render-loop smoke test; the
 * renderer bootstrap lands in the "render-loop smoke test" task, and world
 * content arrives with the town-data phase.
 */
export {};
