Task: Pickup, spawn and the mission frame (Phase 3 task 1).

Moved into game.ts: absorb, startPark, startPuppy, lightFire, lightOrder, headToGarbageTruck, tickFireMission, tickOrderMission, tickParkMission, tickPuppyMission, tickPacers, activate, swapVehicle, and the leaf helpers firePoint/distanceToFire/orderPoint/distanceToOrder/nearestLitterPoint/distanceToLitter/ownerPoint — plus the mission registry and its four contributions. main.ts is down from 1,254 to 492 lines and keeps only the edge: renderer, scene, camera rig, hold gate, parent panel, install hint, input router, audio and the wiring.

In-flight refinements (closures that could not be left behind): the registry had to move with the rules because tickPacers needs missions.isBusy(); serveArmedNow, deliverPuppy, advanceDoorRun, clearPuppyStage, the door-run state and the frameCarPosition state came with tickOrderMission and tickPuppyMission, which call them. GameDeps grew a driven promise so main.ts can wire the input router on the same edge it did inline (motor exists, car model still loading), and GameHud grew setActive/setAbility/setAbilityVisible/setPolicePulse alongside setAbilityBusy. The port stays a set of late-bound closures in main.ts, which is what the old hud?. guards were.

Frame order: game.advance owns wanderers -> motor -> actor -> feedback stack -> fleet clock -> HUD busy edge. The DOM chrome (hold gate, parent panel, install hint) now runs immediately after that instead of immediately before the fleet clock; it shares no state with the fleet or the feedback stack, so nothing child-visible moves. Camera order is untouched: rig.setTarget and followSun stay inside tickVehicle, rig.update still runs last.

Red/green: 14 tests written first and failing (absorb respond/morph, sweep vs drive-over gulp, completion once, atomic park/puppy/fire/order starts, registry tick order, pacer routing, busy pause, activate, swapVehicle build-before-remove, serveArmedNow, the pup door run), then green at 24 tests in the file. Coverage game.ts: 83.19% stmts / 64.96% branches / 82.35% funcs — the uncovered branches are exactly Phase 3 task 2's contract surface (the registry tap contributions, the ability/siren path and the helper hand), which its tests close. Gates: biome 150 files clean, tsc clean, 818/818 tests (66 files).
