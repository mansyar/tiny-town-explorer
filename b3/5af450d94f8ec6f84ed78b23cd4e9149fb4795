Phase 3 Task 1: Marker adapter contract (FR2). Created src/game/mission/missionMarkers.ts: MarkerAdapter interface (showIn/armIn/taps as data) + markerVisible/markerArmed/markerTap (first-match, 'ignore' fallback). Added per-mission adapter configs: FIRE_FLAME (missionManager), ORDER_CONE (iceCreamMission), PARK_FIELD (parkMission), PUPPY_PAW + PUPPY_HEART (puppyMission). Red-first: missionMarkers.test.ts with 16 tests pinning shared contract + every adapter table to the Phase 1 characterization expectations (incl cross-adapter isolation). Coverage missionMarkers.ts: 100% stmts/branch/funcs. One test expectation corrected during green: order respond never required the arm wire, only serve does (adapter matched resolveOrderTap all along). Wiring not re-pointed yet (Task 2). Gates: pnpm check, pnpm typecheck, CI=true pnpm test green (584 tests / 47 files).
