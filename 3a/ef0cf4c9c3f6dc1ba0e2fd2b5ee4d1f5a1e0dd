Task: Gate gameplay input and HUD readiness + Handle initial mount failure safely. main.ts now appends the boot overlay before any async work, wires one-shot first-gesture audio unlock at window level so a tap on the loading overlay still unlocks the context, and holds both game.driven and game.ready inside a single try/catch. Gameplay pointer routing and the vehicle HUD are created only after ready, so taps during loading are absorbed by the overlay and never become queued routes, honks, mission claims, or vehicle selections. On failure the render loop stops, the ResizeObserver disconnects, audio disposes, and the overlay switches to the icon-only retry control that triggers exactly one full-page reload; a settled failure is never blanked and a ready boot is never overwritten. Verified in Chromium: healthy boot clears the overlay; delayed GLB shows the loading toy with the base visible and no HUD; blocked GLB shows retry with a stopped render loop; blocked bark.mp3 still reaches ready with zero unhandled rejections; post-ready canvas taps are unobstructed. pnpm check, pnpm typecheck, and 110 targeted tests passed. main.ts is DOM glue, exempt from unit coverage per workflow.md and manually verified here.
