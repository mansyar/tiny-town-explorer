Task: Wire the browser edge and failure recovery. main.ts appends the zero-text boot overlay before any async work, attaches one-shot first-gesture audio unlock at window level so a tap on the loading overlay still unlocks the context, holds game.driven and game.ready inside a single try/catch, and creates gameplay pointer routing plus the vehicle HUD only after ready. On initial mount failure it stops the render loop, disconnects the ResizeObserver, disposes audio, and switches the overlay to one large icon-only retry control wired to exactly one full-page reload; a settled failure is never blanked and a ready boot is never overwritten by a late optional-audio rejection. Follow-up fba6c30 gave the loading toy the same chunky circular weight as the HUD buttons so it stays legible over sky, road, or grass. Phase 4 checkpoint: user confirmed the automated report, the Chromium evidence, and the physical-iPad plan. pnpm check passed (162 files); pnpm typecheck passed; CI=true pnpm test passed (882 tests across 69 files). Browser evidence: normal boot clears the overlay with 0 console errors; delayed GLB shows the loading toy over the visible base with no HUD; blocked GLB shows retry with a verifiably stopped render loop; one retry tap causes exactly one reload; blocked bark.mp3 still reaches ready with 0 unhandled rejections; post-ready canvas taps are unobstructed; scene inventory 49,428 triangles sits inside the documented range with no duplicated town. main.ts and bootOverlay.ts are DOM glue, exempt from unit coverage per workflow.md and manually verified.
