Phase 2 controller arbitration checkpoint. game.ts now assigns destination generations before mission awaits, serializes vehicle actor requests through an explicit pending queue, commits fleet/HUD/actor state only after replacement readiness, and retains the last known-good actor on load or commit failure. Mission handlers, helper/siren, park pickup, and HUD selection share the policy; inputRouter remains unchanged. missionRegistry gained an optional per-tap context so a failed claimed morph suppresses only that tap's route while preserving the atomic mission claim. Added deterministic deferred regression coverage for stale destinations, fire/ice-cream/park morph races, rollback/retry, mixed mission/HUD selection, and helper parity. Verified pnpm check, pnpm typecheck, targeted 59-test suite, full 862-test suite, coverage (91.3% statements / 87.7% branches overall; game.ts 93.24% / 84.36%), and pnpm build. The pre-existing Vite JavaScript chunk-size warning remains out of scope. Phase 3 browser/iPad verification is still pending.
