Task: Add the zero-text boot overlay to the page edge. Added src/game/hud/bootOverlay.ts (DOM glue, excluded from coverage like the other HUD chrome) with a soft bouncing toy car while loading and one large round-arrow retry button on failure; the retry button carries an aria-label rather than visible text. Added .boot/.boot__toy/.boot__retry styles to the inline index.html sheet following the project's class-based convention. The overlay is appended before the game is created so it exists ahead of all async model work, and its pointer-events:auto surface both takes the first-gesture tap and stops early taps reaching the world. Manual browser verification follows in the wiring task.
