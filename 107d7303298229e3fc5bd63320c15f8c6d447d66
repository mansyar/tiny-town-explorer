Install hint landed: 11 tests, 366 total. Pure shouldShowHint (not installed + not hinted this session), hintFrame (fade in 0.4s, hold, fade out 0.6s, 6s life, bob), platformFrom (iOS/iPad-reporting-as-Mac vs other) with the element as a thin layer. Session-scoped via sessionStorage with a private-mode fallback. Wired into main.ts with the standalone check (display-mode + navigator.standalone) and ticked in advance(). Gate run: biome 73 files clean, tsc clean, 366 tests. Browser verification blocked: this session's browser connection got 502 Bad Gateway on Vite module fetches (identical URLs return 200 over plain HTTP) so the app never booted and the hint element could not be checked in-page. Also included the vitest.config.ts hud coverage exemption that had been left uncommitted since Phase 5.
