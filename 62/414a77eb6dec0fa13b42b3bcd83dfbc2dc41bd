MissionManager FSM landed as pure logic: 13 tests - the whole state walk (idle, spawned, driving, active, complete, back to idle) plus illegal-transition guards, hose arming by distance in both directions, and a burst countdown of 3-4 from an injected roll. No renderer and no timers beyond the resolution linger, so a full mission runs in milliseconds in a test. Task 1 of Phase 6; pacing (when a fire appears and where) is the next task and deliberately stays out of this module.
