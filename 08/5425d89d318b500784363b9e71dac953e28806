Task: Author and mount the corner shop (FR3). Changes: scripts/blender-corner-shop.py (deterministic recipe copied from the ice-cream-truck canonical, 17 contracted shop_* nodes, front authored +Y and turned 180 about Z so the GLB faces glTF +z like the kit); packed GLB at src/assets/kits/city-kit-suburban/corner-shop.glb with embedded city-kit-suburban/colormap; BuildingKind 'shop' + BUILDING_EXTENTS {width 1.56, depth 1.38} (measured); modelRegistry entry (joins the precache via the BUILDING_MODELS spread); house-15 at (6,6) facing north as the junction-corner landmark; test fallout (house counts 15, figureEight block now expects the shop as a house, map title). Gate 1.1 measurement table lives in the recipe docstring. Measured: extents 1.560 x 1.380 x 0.900, min z 0.000, 29.2 KiB raw / 0.04 MiB packed; fit 0.551 puts the awning-side wall at ~0.62 from the street centre line (family band 0.574-0.748). Palette = the kit's own atlas texels (family green/coral columns + neutrals). Three render iterations; the user accepted the woven-stripe awning at Layer 3 (2026-09-24). Gates: pnpm check clean, pnpm typecheck clean, CI=true pnpm test 63 files / 779 tests green. Why: FR3 - the second district's zero-text landmark that mounts like any house, so fire, ice-cream and the puppy treat it with no special cases.

Phase 3 verification report. Test command: CI=true pnpm test - 63 files, 779 tests, all passing. Coverage on touched logic: townTypes.ts 100%, modelRegistry.ts 100%, townMap.ts 100% (Phase 2 run, unchanged), puppySpots.ts 97.6% - all above the 80% gate (scripts/blender-corner-shop.py is build tooling, exempt like blender-analyze-kit.py). Gates: pnpm check clean, pnpm typecheck clean. Manual verification (user-confirmed 2026-09-24): the corner shop stands at the junction corner as the second district's landmark - reads 'shop' at play distance and 48px with zero text, rests on the ground with no float or sink, matches the kit family at slightly grander landmark scale, treats every mission as a house (fire lands on it, ice-cream orders originate there, the puppy's owner door works), precached for offline, no regressions.
