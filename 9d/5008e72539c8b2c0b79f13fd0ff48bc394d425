Task: Input routing (tap ordering house = respond when spawned, serve when active + armed; tap truck ability = jingle; newest-wins preserved).

main.ts work, all glue over already-tested rules: resolveOrderTap/isTapOnHouse decide what a tap on the ordering house means (rules landed with orderFlow.test.ts, 15 tests, in d1a5e4f); answerMissions() splits the mission-answering block out of tapAt so the destination routing, fleet.interruptBurst and newest-wins path are untouched; the ability press records a jingle in the serve latch; every morph now goes through one activate() helper that tells the latch, so morphing away abandons the serve (AC2) and a fresh jingle is needed; and tickOrderMission blooms the target ring on the house on the frame serve first arms. serveArmedNow() is the single source for both the ring and the serve tap, so the affordance and the honoured action cannot drift apart.

Files: src/main.ts. Verification: 412 tests / 34 files green; pnpm check + typecheck clean (tapAt split into answerMissions when biome flagged cognitive complexity 17, now inside the 15 budget). Manual verify recorded for the device pass: tap the ordering house morphs to the truck and drives over; jingle with the ability button, ring blooms on arrival, tap the house and one cone changes hands; tapping any other house just drives.
