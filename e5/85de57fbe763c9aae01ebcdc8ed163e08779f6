Task: Implement the two-phase town mount. Added optional TownMountOptions.onBaseReady; mountTown now mounts all ground placements first, signals the base before awaiting models, and then adds model placements in authored order. Rejection propagation, transforms, fitting, footprints, and disposal remain intact. Targeted tests: 16 passed. Scoped townRenderer.ts coverage: 100% statements, branches, functions, and lines.
