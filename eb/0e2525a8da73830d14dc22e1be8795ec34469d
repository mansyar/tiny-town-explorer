Task: Make sampled audio failure non-blocking. Added src/game/audio/sampleLoader.ts exporting loadSamples, an allSettled aggregate over the existing AudioEngine.load contract, and switched main.ts from Promise.all to it so a failed fetch or decode cannot reject or escape at boot. Registry, URLs, decode path, synthesized voices, mute behavior, and first-gesture unlock are unchanged; a failed sample plays silent and a later load can still populate it. Targeted audio tests: 20 passed. Scoped coverage: sampleLoader.ts 100% statements/branches/functions/lines. Observed pre-existing gap (not in scope): audioEngine.ts sits at 54% because its existing tests cover the pure schedules only.
