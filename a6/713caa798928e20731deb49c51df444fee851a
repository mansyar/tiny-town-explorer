Phase 1 red baseline: completed deterministic coverage for newest destination, atomic fire/ice-cream/park morphs, failed morph rollback/retry, serialized HUD selection and mission precedence, and helper-hand parity. Corrected test fixtures for park setup, helper vehicle state, and boot actor call counts. CI=true pnpm test -- src/game/game.test.ts reports 59 tests with 50 passing and 9 intentional arbitration failures; pnpm check and pnpm typecheck pass.
