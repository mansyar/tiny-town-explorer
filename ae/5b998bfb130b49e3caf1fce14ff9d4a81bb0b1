Task: Apply review suggestions (Review Fixes phase, from the conductor-review of track 'ice-cream-delivery_20260922').

Findings addressed, in review order:
1. High - serve tap could honk: a mission tap was judged against command.target, which inputRouter had already snapped to any prop within PROP_SNAP_RADIUS (0.45), and the 0.5 dead-zone honk was measured from the same point. Reproduced once on the desktop pass (tap on the ordered lot resolved to cone-3). Fix: DriveCommand now carries `landed` (the pre-snap ground point) beside `target`, tapAt takes (point, aim = point), and answerMissions judges both missions against the aim. Two new router tests pin it: the aim survives a prop snap, and aim == target when nothing snapped. The fire mission had the same exposure and is fixed with it.
2. Medium - .hud-button--ability.is-hidden had no CSS rule anywhere, so the v1 "the ability button is the hose button, so it only exists once the car is close enough" never reached the screen. Fix: index.html gains the rule (opacity 0, pointer-events none) with a comment explaining why an open ice-cream order keeps the button visible.
3. Medium - iceCreamPacer.ts was a near-verbatim copy of firePacer.ts (normalized residual diff: 60 lines, all comments except two identifiers). Fix: new src/game/mission/calmGapPacer.ts owns the shared rule parameterized by minSeconds/maxSeconds/minDistance; firePacer and iceCreamPacer are thin mission-named wrappers that keep every existing export, so no caller or existing test changed. New calmGapPacer.test.ts (10 tests) covers the parameterised paths the per-mission suites cannot reach (instant missions, a distance wider than the town, minDistance 0, an empty town). One of those tests caught a wrong expectation of mine - the fallback never repeats the previous house unless it is the only one - and the test was corrected, not the code.
4. Low - two names for one tolerance: SNAP_TO_ORDER (orderFlow) and snapToFire (main.ts) are now one exported MISSION_SNAP_RADIUS. MissionFocusInput's optional coordinates are `?` rather than required `| undefined` (exactOptionalPropertyTypes is off, so call sites are unchanged).
Docs: docs/playtest.md's "known issues to watch for" is now "issues the pass turned up, and what became of them", with the fixed behaviour and what to look at on the device; AC7's counts refreshed; the v1 device step notes that the hose button now arrives with proximity.

Verification: pnpm check clean (88 files), pnpm typecheck clean, CI=true pnpm test 433 tests / 36 files green, coverage all files 82.36% statements with game/mission 97.55% (calmGapPacer, firePacer, iceCreamPacer, orderFlow, missionFocus all 100% statements).

Files: index.html, src/main.ts, src/game/input/inputRouter.ts, src/game/input/inputRouter.test.ts, src/game/mission/{calmGapPacer,firePacer,iceCreamPacer,orderFlow,missionFocus}.ts, src/game/mission/calmGapPacer.test.ts, docs/playtest.md.
